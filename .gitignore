node_modules
.pnpm-store
.skill-build
.next
.next-prod
.next-prod-broken-*
.next_corrupt_*
.next_mixed_*
.next_prevercel_backup_*
dist
output
.env
.env.*
.local
coverage
.DS_Store
*.tsbuildinfo
eval "$(ssh-agent -s)"
eval "$(ssh-agent -s)".pub

# harness scratch — drafts/reviews are local-only, outputs may be committed selectively
_workspace/drafts/*
!_workspace/drafts/.gitkeep
_workspace/reviews/*
!_workspace/reviews/.gitkeep
